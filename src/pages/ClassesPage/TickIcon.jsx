import { IconCircleCheck } from "@tabler/icons-react";

//? it might be better to have the benefits as a Mantine List with the TickIcon as the icon
const TickIcon = () => <IconCircleCheck color="#fc8a08" size={28} />;

export default TickIcon;
